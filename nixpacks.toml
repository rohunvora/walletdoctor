# nixpacks.toml
[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "gunicorn -b 0.0.0.0:$PORT wallet_analytics_api:app --timeout 120 --workers 2" 